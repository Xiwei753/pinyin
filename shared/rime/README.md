# Rime 共享配置

这是项目的 Rime 基础配置目录。

## 注意事项

- 当前 `xiwei_pinyin.dict.yaml` 只是一个最小可用测试词库（用于验证架构和构建流程），不是正式的大词库。请勿直接用于日常高强度输入。
- 九键方案 `xiwei_t9.schema.yaml` 是初步的实验性实现。

请在 `shared/settings/user.yaml` 配置并使用 `tools/sync/` 中的工具拉取个人的真实词库。
