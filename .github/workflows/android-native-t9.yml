name: Android Native T9 Build

on:
  push:
    paths:
      - 'frontends/android-ime/native-app/**'
      - 'tools/dictionary/**'
      - 'third_party/rime-ice/**'
      - '.github/workflows/android-native-t9.yml'
  pull_request:
    paths:
      - 'frontends/android-ime/native-app/**'
      - 'tools/dictionary/**'
      - 'third_party/rime-ice/**'
      - '.github/workflows/android-native-t9.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Check Dictionary Files
        run: |
          if [ ! -f "third_party/rime-ice/cn_dicts/base.dict.yaml" ]; then
            echo "Error: base.dict.yaml not found"
            exit 1
          fi
          if [ ! -f "third_party/rime-ice/cn_dicts/8105.dict.yaml" ]; then
            echo "Error: 8105.dict.yaml not found"
            exit 1
          fi

      - name: Run Build Dictionary Assets Script
        run: |
          python3 tools/dictionary/build_t9_assets.py

      - name: Verify Generated Dictionary Is Committed
        run: |
          git diff --exit-code frontends/android-ime/native-app/android/app/src/main/assets/t9_source_dict.tsv
          git diff --exit-code frontends/android-ime/native-app/android/app/src/main/assets/t9_dict.db

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x frontends/android-ime/native-app/android/gradlew

      - name: Build Debug APK
        run: |
          cd frontends/android-ime/native-app/android
          ./gradlew test assembleDebug

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: xiwei-t9-debug-apk
          path: frontends/android-ime/native-app/android/app/build/outputs/apk/debug/app-debug.apk
